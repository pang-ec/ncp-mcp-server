{
  "name": "ncp-compute-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for NCP Compute management",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ncp-compute-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
